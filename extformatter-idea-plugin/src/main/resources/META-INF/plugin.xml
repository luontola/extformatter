<!DOCTYPE idea-plugin PUBLIC "Plugin/DTD" "http://plugins.intellij.net/plugin.dtd">
<idea-plugin>

    <name>${plugin.name}</name>
    <id>${plugin.id}</id>
    <version>${project.version}</version>

    <description>
        Allows using external code formatters, especially Eclipse's code formatter, from within IntelliJ IDEA.
        Solves the problem of maintaining a common code style in team environments where both IDEA and Eclipse
        are being used.
    </description>
    <category>Formatting</category>

    <vendor url="http://www.orfjackal.net">Esko Luontola</vendor>

    <idea-version since-build="7364"/>

    <change-notes><![CDATA[
        <p><b>0.2.0</b> (2007-xx-xx)
        <br>- xx</p>
        
        <p><b>0.1.0</b> (2007-12-05)
        <br>- Initial release</p>

        <p><b>Known issues / Coming soon</b>
        <br>- No help documentation, especially on how to get the "org.eclipse.jdt.core.prefs" file from Eclipse.
        <br>- Reformatting many files is slower than it could be, because for now Eclipse is started once for each individual file.
        <br>- No support for a generic command line formatter.
        <br>- Does not support reformatting a selection within a file.
        </p>
    ]]></change-notes>

    <project-components>
        <component>
            <implementation-class>net.orfjackal.extformatter.plugin.ProjectSettingsComponent</implementation-class>
        </component>
    </project-components>

</idea-plugin>
